# name: Lineaje AI Policy Scan

# on:
#   workflow_dispatch:

# jobs:
#   scan:
#     runs-on: self-hosted
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: '3.12'

#       - name: Install dependencies
#         run: pip install mcp httpx

#       - name: Run Lineaje AI policy scan
#         env:
#           LINEAJE_PAT_TOKEN: ${{ secrets.LINEAJE_PAT_TOKEN }}
#           GH_TOKEN: ${{ secrets.GH_TOKEN }}
#           MCP_TLS_INSECURE_SKIP_VERIFY: "1"
#         run: |
#           python .github/scripts/scm_unifai_scan.py \
#             --source-path . \
#             --repo ${{ github.repository }} \
#             --branch ${{ github.ref_name }} \
#             --head-sha ${{ github.sha }} \
#             --exclude .git \
#             --mcp-server-url http://172.206.26.109/mcp \
#             --create-fix-pr

name: Lineaje UnifAI Scan

on:
  workflow_dispatch:
    inputs:
      create_fix_pr:
        description: "Open a remediation PR with the suggested fixes"
        type: boolean
        default: true

permissions:
  contents: write
  pull-requests: write

jobs:
  lineaje-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install mcp==1.27.1

      - name: Prepare scanner
        run: |
          # Preserve the scanner before removing .github from scan input
          cp .github/scm_unifai_scan.py /tmp/scm_unifai_scan.py

          # Exclude GitHub config/workflows/scripts and git internals from the scan
          rm -rf .github .git

      - name: Run Lineaje AI policy scan
        env:
          LINEAJE_PAT_TOKEN: ${{ secrets.LINEAJE_PAT_TOKEN }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MCP_TLS_INSECURE_SKIP_VERIFY: "1"
        run: |
          python /tmp/scm_unifai_scan.py \
            --source-path . \
            ${{ inputs.create_fix_pr && '--create-fix-pr' || '' }} >> "$GITHUB_STEP_SUMMARY"
